{
  "name": "connected-service",
  "version": "1.0.0",
  "description": "This microservice is designed to retrieve the last session (connection) timestamp of a user. It integrates two databases: PostgreSQL (for user data) and MongoDB (for session data). It exposes RESTful API endpoints and includes a load-balancer-friendly health check.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "moment": "^2.30.1",
    "mongoose": "^8.12.1",
    "pg": "^8.13.3"
  },
  "devDependencies": {}
}
